import 'package:flutter/material.dart';


final ThemeData darkMode = ThemeData(
  brightness: Brightness.dark,
  colorScheme: ColorScheme.dark(
    surface: Color(0xFF0A192F), // Deep Navy Blue (Background)
    primary: Color(0xFF1B3A4B), // Ocean Blue (Main elements)
    secondary: Color(0xFF3B8EA5), // Teal Blue (Cards & Highlights)
    inversePrimary: Color(0xFFF5F5DC), // Soft Beige (Text & Icons)
  ),

);
